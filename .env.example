# Rohlik.cz credentials.
# Per-group config in groups/<group>/container.json is what the agent actually
# reads; these are here for reference and for tooling that expects them.
ROHLIK_USERNAME=
ROHLIK_PASSWORD=
ROHLIK_BASE_URL=https://www.rohlik.cz
